fastapi>=0.115
uvicorn[standard]>=0.30
# Pin a stable release. Newer 1.63.0 bundles can omit a dynamically imported
# browser chunk on Render, causing a blank page / ChatInput import failure.
streamlit==1.50.0
langgraph>=0.2
langchain-core>=0.3
pydantic>=2.8
PyYAML>=6.0
httpx>=0.27
python-dotenv>=1.0
pytest>=8.3
pytest-asyncio>=0.24
